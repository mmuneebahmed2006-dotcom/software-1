import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";

interface Props { open: boolean; title: string; folder: string; folders: string[]; onTitle: (value: string) => void; onFolder: (value: string) => void; onCancel: () => void; onSave: () => void }
export function SaveDocumentDialog({ open, title, folder, folders, onTitle, onFolder, onCancel, onSave }: Props) {
  return <Dialog open={open} onOpenChange={(value) => !value && onCancel()}><DialogContent><DialogHeader><DialogTitle>Save as new document</DialogTitle><DialogDescription>Choose the name and folder used in the current document category.</DialogDescription></DialogHeader><label className="dialog-field"><span>Document name</span><Input autoFocus value={title} onChange={(event) => onTitle(event.target.value)} aria-label="Save document name"/></label><label className="dialog-field"><span>Folder</span><select value={folder} onChange={(event) => onFolder(event.target.value)} aria-label="Save document folder">{folders.map((entry) => <option key={entry}>{entry}</option>)}</select></label><DialogFooter><Button type="button" variant="outline" onClick={onCancel}>Cancel</Button><Button type="button" disabled={!title.trim()} onClick={onSave}>Save Document</Button></DialogFooter></DialogContent></Dialog>;
}