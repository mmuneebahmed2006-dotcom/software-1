name: Build Windows EXE

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install Bun
        uses: oven-sh/setup-bun@v1

      - name: Install Dependencies
        run: bun install

      - name: Build Web Application
        run: bun run build

      - name: Prepare Static Files
        run: node prepare-static.cjs

      - name: Verify app-output folder
        run: |
          echo "Listing app-output folder:"
          dir app-output
          echo "Listing app-output/server folder:"
          dir app-output\server

      - name: Build Executable
        run: bunx electron-builder --windows nsis
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Verify packaged resources
        run: |
          echo "Listing dist folder:"
          dir dist
          echo "Listing unpacked resources folder:"
          dir dist\win-unpacked\resources

      - name: Upload EXE Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Software-Setup-EXE
          path: dist/*.exe
